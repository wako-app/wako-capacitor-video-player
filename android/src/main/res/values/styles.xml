<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Style pour le menu popup avec opacité -->
    <style name="PopupMenuStyle" parent="Theme.AppCompat">
        <item name="android:popupBackground">#80000000</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:itemBackground">@android:color/transparent</item>
        <item name="android:textAppearance">@style/PopupMenuTextAppearance</item>
    </style>
    
    <!-- Style pour le texte du menu popup -->
    <style name="PopupMenuTextAppearance">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
</resources>
